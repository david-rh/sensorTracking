%prod.kafka.bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
%dev.kafka.bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS:sensorkafka-kafka-tls-bootstrap-sensortracking.apps.ussredhat.dbarh.demo:443}
%dev.quarkus.http.port=8080
mp.messaging.incoming.sensor-data.topic=sensor-data
kafka.security.protocol = SSL
kafka.ssl.truststore.location = ${KAFKA_SSL_TRUSTSTORE_LOCATION:/home/david/Work/Demos/sensorTracking/mykeystore.jks}
kafka.ssl.truststore.password = ${KAFKA_SSL_TRUSTSTORE_PASSWORD:sensorcapass}